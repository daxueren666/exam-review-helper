# Exam-Review-Helper dependencies
#
# Install: pip install -r requirements.txt
# First run downloads ~200MB docling model files (one-time)

# === PDF extraction (core) ===
docling>=2.10.0
PyMuPDF>=1.23.0
rapidocr-onnxruntime>=1.3.0

# === Multi-format extraction (DOCX/TXT/MD) ===
# Microsoft MarkItDown: DOCX to markdown (uses mammoth internally, v0.1.6 released 2026-05, MIT)
# Only for DOCX - markitdown does not detect encoding for TXT, Chinese GBK would garble
markitdown[docx]>=0.1.6
# charset-normalizer: TXT/MD encoding detection (UTF-8/GBK/GB2312/Big5/GB18030)
# Critical for Chinese textbooks - markitdown does not do this, must install separately
charset-normalizer>=3.4.0

# === docling indirect deps (must pin, otherwise import fails) ===
# python-pptx: docling's PowerPoint backend imports pptx even when only processing PDF
# transformers>=5.0: docling 2.10+ layout model rt_detr_v2 requires it
python-pptx>=1.0.0
transformers>=5.0.0

# === Config ===
PyYAML>=6.0

# === Testing ===
pytest>=7.4.0
pytest-cov>=4.1.0
