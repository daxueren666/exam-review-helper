# Exam-Review-Helper 依赖
#
# 安装：pip install -r requirements.txt
# 首次运行会下载约 200MB docling 模型文件（一次性）

# === PDF 提取（核心）===
docling>=2.10.0
PyMuPDF>=1.23.0
rapidocr-onnxruntime>=1.3.0

# === docling 间接依赖（必须 pin，否则 import 失败）===
# python-pptx：docling 的 PowerPoint 后端会 import pptx，即使只处理 PDF
# transformers>=5.0：docling 2.10+ 的 layout 模型 rt_detr_v2 需要
python-pptx>=1.0.0
transformers>=5.0.0

# === 测试和代码质量 ===
pytest>=7.4.0
pytest-cov>=4.1.0
